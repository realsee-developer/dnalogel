import * as THREE from 'three';
import type { ObjectFit } from '../../PanoTagPlugin';
export default function getPositionsByObjectFit(objectFit: ObjectFit, originPositions: THREE.Vector3[], originRatio: number): THREE.Vector3[];
